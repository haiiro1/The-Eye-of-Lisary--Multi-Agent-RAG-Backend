FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir streamlit requests pymupdf

COPY Test/gui_test.py .

EXPOSE 8501

CMD ["streamlit", "run", "gui_test.py", "--server.port=8501", "--server.address=0.0.0.0"]
